package pw.dotdash.director.core.value

/**
 * A marker interface to denote that a value parameter is transparent.
 * Transparent parameters consume no tokens and show no usage.
 */
interface TransparentParameter